import Menu from "./menu/Menu";
import Options from "./options/Options";

const Header = () => {
  return (
    <header>
      <Menu />
      <Options />
    </header>
  );
};

export default Header;
