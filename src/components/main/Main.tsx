import WeatherPage from "../pages/WeatherPage";

// component wraps content into HTML5 main tag

const Main = () => {
  return (
    <main>
      <WeatherPage />
    </main>
  );
};

export default Main;
